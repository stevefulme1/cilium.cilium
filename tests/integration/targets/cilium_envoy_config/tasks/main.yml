---
# Integration tests for stevefulme1.cilium.cilium_envoy_config

- name: List cilium_envoy_config resources
  stevefulme1.cilium.cilium_envoy_config_info:
    kubeconfig: "{{ test_kubeconfig }}"
    namespace: "{{ test_namespace | default('kube-system') }}"
  register: list_result

- name: Verify cilium_envoy_config listing
  ansible.builtin.assert:
    that:
      - list_result is not failed

- name: Create cilium_envoy_config (check_mode)
  stevefulme1.cilium.cilium_envoy_config:
    kubeconfig: "{{ test_kubeconfig }}"
    namespace: "{{ test_namespace | default('kube-system') }}"
    name: "ansible-test-{{ resource_prefix }}"
    state: present
    argument_spec: "ansible-test-argument_spec"
    description: "ansible-test-description"
  check_mode: true
  register: check_result

- name: Verify check_mode reports changed
  ansible.builtin.assert:
    that:
      - check_result is changed

- name: Create cilium_envoy_config
  stevefulme1.cilium.cilium_envoy_config:
    kubeconfig: "{{ test_kubeconfig }}"
    namespace: "{{ test_namespace | default('kube-system') }}"
    name: "ansible-test-{{ resource_prefix }}"
    state: present
    argument_spec: "ansible-test-argument_spec"
    description: "ansible-test-description"
  register: create_result

- name: Verify cilium_envoy_config was created
  ansible.builtin.assert:
    that:
      - create_result is changed

- name: Create cilium_envoy_config again (idempotency)
  stevefulme1.cilium.cilium_envoy_config:
    kubeconfig: "{{ test_kubeconfig }}"
    namespace: "{{ test_namespace | default('kube-system') }}"
    name: "ansible-test-{{ resource_prefix }}"
    state: present
    argument_spec: "ansible-test-argument_spec"
    description: "ansible-test-description"
  register: idem_result

- name: Verify idempotent create
  ansible.builtin.assert:
    that:
      - idem_result is not changed

- name: Delete cilium_envoy_config (check_mode)
  stevefulme1.cilium.cilium_envoy_config:
    kubeconfig: "{{ test_kubeconfig }}"
    namespace: "{{ test_namespace | default('kube-system') }}"
    name: "ansible-test-{{ resource_prefix }}"
    state: absent
    argument_spec: "ansible-test-argument_spec"
    description: "ansible-test-description"
  check_mode: true
  register: delete_check

- name: Verify check_mode delete reports changed
  ansible.builtin.assert:
    that:
      - delete_check is changed

- name: Delete cilium_envoy_config
  stevefulme1.cilium.cilium_envoy_config:
    kubeconfig: "{{ test_kubeconfig }}"
    namespace: "{{ test_namespace | default('kube-system') }}"
    name: "ansible-test-{{ resource_prefix }}"
    state: absent
    argument_spec: "ansible-test-argument_spec"
    description: "ansible-test-description"
  register: delete_result

- name: Verify cilium_envoy_config was deleted
  ansible.builtin.assert:
    that:
      - delete_result is changed

- name: Delete cilium_envoy_config again (idempotent)
  stevefulme1.cilium.cilium_envoy_config:
    kubeconfig: "{{ test_kubeconfig }}"
    namespace: "{{ test_namespace | default('kube-system') }}"
    name: "ansible-test-{{ resource_prefix }}"
    state: absent
    argument_spec: "ansible-test-argument_spec"
    description: "ansible-test-description"
  register: delete_idem

- name: Verify idempotent delete
  ansible.builtin.assert:
    that:
      - delete_idem is not changed
